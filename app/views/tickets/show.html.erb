<div id='ticket'>
  <%= link_to "Edit Ticket", [:edit, @project, @ticket] %>
  <%= link_to "Delete Ticket", [@project, @ticket], :method => :delete, :confirm => "Really?" %> 
  <h2><%= @ticket.title %></h2>

  <%= simple_format(@ticket.description) %>
</div>
